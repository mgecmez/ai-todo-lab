# Release Notes — v1.0.0 Sprint 3 (BL-030 + BL-031)

**Release Date:** 2026-03-22
**Type:** Feature Release
**Scope:** Mobile (Frontend only)

---

## Overview

Sprint 3 delivers the first-launch onboarding experience and establishes the app icon/splash screen infrastructure. New users are now greeted with a 5-slide introduction before reaching the auth screen. The splash screen color now matches the app's gradient theme.

---

## What's New

### 5-Slide Onboarding Flow (BL-030)

- **First-launch only:** Onboarding is shown once; an AsyncStorage flag (`@app:onboarding:done`) ensures it is never shown again after completion
- **Skip anytime:** A Skip button on slides 1–4 jumps directly to slide 5 without writing the flag
- **Get Started slide:** Slide 5 has two CTA buttons — "Hesap Oluştur" (Register) and "Giriş Yap" (Login) — which write the flag and navigate to the corresponding auth screen
- **Pill indicator:** Active slide shown as a wide pill (~33px), inactive slides as narrow pills (~18px)
- **i18n ready:** All strings use `t()` — Turkish and English translations included

| Slide | Icon | Content |
|-------|------|---------|
| 1 — Welcome | `checkmark-circle` | App name + tagline |
| 2 — Task Management | `checkmark-done-circle-outline` | Create, prioritize, pin |
| 3 — Due Dates & Reminders | `alarm-outline` | Deadlines + local notifications |
| 4 — Offline-First | `cloud-offline-outline` | Works without internet, auto-syncs |
| 5 — Get Started | — | Register / Login CTA |

### App Icon & Splash Screen Configuration (BL-031)

- `app.json` icon, splash, and Android adaptive icon fields configured
- Splash background color updated to `#05243E` (matches `colors.gradientBottom`)
- Placeholder assets added for `splash.png` and `adaptive-icon.png`
- Real logo/brand assets deferred to the EAS Build sprint (BL-033)

---

## Technical Changes

### New Files

| File | Description |
|------|-------------|
| `mobile/src/screens/OnboardingScreen.tsx` | 5-slide onboarding component |
| `mobile/assets/splash.png` | Placeholder splash screen asset |
| `mobile/assets/adaptive-icon.png` | Placeholder Android adaptive icon |
| `docs/onboarding-spec.md` | Feature specification |
| `docs/onboarding-architecture.md` | Technical architecture document |
| `tasks/014-onboarding.md` | Sprint task list (8 tickets) |

### Modified Files

| File | Change |
|------|--------|
| `mobile/src/navigation/RootNavigator.tsx` | Added `onboardingDone` state; reads `ONBOARDING_KEY` from AsyncStorage on mount; renders `OnboardingScreen` when flag is not set |
| `mobile/src/services/cache/cacheKeys.ts` | Added `ONBOARDING_KEY = '@app:onboarding:done'` |
| `mobile/src/i18n/locales/tr.json` | Added `onboarding.*` section (12 keys) |
| `mobile/src/i18n/locales/en.json` | Added `onboarding.*` section (12 keys) |
| `mobile/app.json` | `splash.image` → `./assets/splash.png`; `splash.backgroundColor` → `#05243E`; `adaptiveIcon.foregroundImage` → `./assets/adaptive-icon.png`; `adaptiveIcon.backgroundColor` → `#05243E` |

---

## Breaking Changes

None. Existing users with the flag already set (via a fresh install over a dev build) will skip onboarding and go straight to auth. Backend API contract is unchanged.

---

## Rules Going Forward

- Any new screen accessible before auth must be added to `RootNavigator` or `AuthNavigator` — not `AppNavigator`
- The `ONBOARDING_KEY` constant must always be imported from `cacheKeys.ts`; inline strings are not permitted
